{
  "name": "wolves-league-design-system",
  "private": true,
  "packageManager": "pnpm@8.6.11",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  }
}
